# Анализ крупных файлов ( > 300 строк)

Дата анализа: 23 января 2026

Цель: Разбить файлы размером более 300-400 строк на меньшие логические части для улучшения читаемости и поддержки.

## Список приоритетных файлов для рефакторинга

| Файл | Строк | Статус | План действий |
|---|---|---|---|
| `Remission/Views/TorrentList/TorrentListView.swift` | 280 (было 567) | ✅ Выполнено | Вынесены `Header`, `Controls`, `EmptyState`, `StorageSummary` в отдельные компоненты. |
| `Remission/Features/TorrentList/TorrentListFeature.swift` | 420 (было 560) | ✅ Выполнено | Вынесена логика фильтрации и сортировки в `+Filtering.swift`. |
| `Remission/Repositories/TorrentRepository.swift` | 270 (было 546) | ✅ Выполнено | Разделен на extension: `+Fetch`, `+Actions`, `+Mutation`, `+Cache`, `+Helpers`. |
| `Remission/Network/Transmission/TransmissionClient.swift` | 380 (было 546) | ✅ Выполнено | Разделен на extension: `+Session`, `+Torrent`, `+System`. Удален старый `+RPCMethods`. |
| `Remission/Storage/ServerSnapshotCache.swift` | 516 | 🔴 Требует действий | Вынести логику `Actor` в отдельные методы или вспомогательный менеджер кеша. |
| `Remission/App/AppDependencies.swift` | 449 | 🟠 Высокий приоритет | Разбить на файлы по доменам (Network, Persistence, System). |
| `Remission/Views/Diagnostics/DiagnosticsView.swift` | 436 | 🟠 Высокий приоритет | Вынести `LogsView`, `ConnectivityView`, `ServerInfoView` в отдельные файлы. |
| `Remission/Features/ServerDetail/ServerDetailFeature.swift` | 421 | 🟠 Высокий приоритет | Вынести логику удаления и сброса доверия в отдельные методы или middleware. |
| `Remission/Views/TorrentAdd/AddTorrentView.swift` | 414 | 🟠 Высокий приоритет | Разделить на `MagnetInputView`, `FilePickerView` и `OptionsSection`. |
| `Remission/Views/Settings/SettingsView.swift` | 405 | 🟠 Высокий приоритет | Вынести секции настроек (Server, Interface, Network) в отдельные View. |
| `Remission/Repositories/SessionRepository.swift` | 405 | 🟠 Высокий приоритет | Выделить маппинг и валидацию в отдельные сущности. |
| `Remission/Features/Settings/SettingsFeature.swift` | 391 | 🟡 Средний приоритет | Проверить возможность выноса логики валидации и сохранения. |
| `Remission/Views/TorrentDetail/TorrentDetailView+Sections.swift` | 372 | 🟡 Средний приоритет | Разбить на еще более мелкие файлы (уже частично сделано, но можно лучше). |
| `Remission/Features/ServerList/ServerListFeature.swift` | 355 | 🟡 Средний приоритет | Вынести логику миграции и загрузки списка. |
| `Remission/Views/ServerDetail/ServerDetailView.swift` | 351 | 🟡 Средний приоритет | Вынести header, stats и action buttons. |

## Стратегия рефакторинга

1.  **View (SwiftUI):**
    *   Принцип: "Один файл - одна View структура (или тесно связанные)".
    *   Метод: Выделение `private var body`-частей в отдельные `struct MySubView: View` в том же файле, а затем перенос в новый файл в папку `Components/`.

2.  **Reducers (TCA):**
    *   Принцип: Разделение ответственности.
    *   Метод: Использование `extension MyReducer` для выноса логики обработки конкретных групп действий (Actions) или вспомогательных функций.
    *   Метод: Вынос сложной логики (фильтрация, сортировка) в отдельные `struct/class` (Helpers).

3.  **Repositories / Services:**
    *   Принцип: Группировка по функциональности.
    *   Метод: Использование `extension` в отдельных файлах (например, `TransmissionClient+Session.swift`, `TransmissionClient+Torrent.swift`).

## Следующий шаг

Приступить к рефакторингу `Remission/Storage/ServerSnapshotCache.swift`.