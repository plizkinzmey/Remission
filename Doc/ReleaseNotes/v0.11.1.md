## What's New

### English
* Diagnostics (macOS): live log updates now work correctly.
* Diagnostics (macOS): the log list no longer "jumps" during updates; new entries are appended smoothly.
* Diagnostics (macOS): improved sheet sizing so the header and buttons stay visible on smaller screens.
* Diagnostics: default log level filter is now **Info**.

### Русский
* Диагностика (macOS): исправлено обновление логов в реальном времени.
* Диагностика (macOS): список логов больше не "скачет" при обновлениях; новые записи добавляются плавно.
* Диагностика (macOS): улучшена высота окна, чтобы заголовок и кнопки оставались видимыми на небольших экранах.
* Диагностика: фильтр уровня логов по умолчанию теперь **Info**.

## Changes (Git Log)
* Merge develop into main (5be7b3c)
* Merge diagnostics macOS fixes (75cb9a7)
* Default diagnostics level filter to Info (f82fa36)
* Fix macOS diagnostics sheet sizing (b7624c3)
* Reduce macOS diagnostics sheet height (47dfd5d)
* Avoid diagnostics log list jumps on macOS (28ade17)
* Fix macOS diagnostics live log updates and remove duplicate header (3ded3b9)

